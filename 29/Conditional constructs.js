const normalizeUrl = (sentence) => {
  const str1 = 'yandex.ru'
  if (str1 === 'https://');
  return 'https://yandex.ru'
  console.log(str1.startsWith('https://'));
}

normalizeUrl('yandex.ru');
export default normalizeUrl;
